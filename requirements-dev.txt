# HealthGuard AI - FULL pipeline dependencies (training, analysis, notebooks)
# Use this to reproduce everything: `pip install -r requirements-dev.txt`
# then `python src/run_pipeline.py`.
# (The lean requirements.txt is only what the deployed dashboard needs.)
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
scipy>=1.11
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2
shap>=0.44
lime>=0.2
matplotlib>=3.8
seaborn>=0.13
plotly>=5.20
streamlit>=1.30
joblib>=1.3
optuna>=3.5
# dev / notebooks
pytest>=7.4
nbformat>=5.9
jupyter>=1.0
