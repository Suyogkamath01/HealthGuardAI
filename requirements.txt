# HealthGuard AI - RUNTIME dependencies (Streamlit Cloud deploy)
# Only what the dashboard imports at runtime. Versions are pinned so the
# committed CatBoost model un-pickles identically to how it was trained.
# For the full training/analysis pipeline, install requirements-dev.txt instead.
streamlit==1.58.0
plotly==6.8.0
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0
joblib==1.5.3
matplotlib==3.10.9
catboost==1.2.10
